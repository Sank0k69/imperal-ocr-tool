{
  "app_id": "ocr-tool",
  "version": "2.1.1",
  "description": "Image-to-text extraction via Tesseract on our server. No AI tokens — 1 action per extraction.",
  "category": "productivity",
  "capabilities": [],
  "tools": [
    {
      "name": "ocr",
      "description": "Drop screenshots, photos, documents, receipts, handwritten notes — get the text back.",
      "scopes": [
        "store.read",
        "store.write",
        "http.request"
      ],
      "parameters": {
        "message": {"type": "string", "required": false},
        "kwargs": {"type": "string", "required": true}
      }
    },
    {
      "name": "__panel__workspace",
      "description": "Panel: OCR workspace (drop zone + result + history + preferences)",
      "scopes": [],
      "parameters": {"params": {"type": "string", "required": false}}
    },
    {
      "name": "__panel__sidebar",
      "description": "Panel: Sidebar (usage stats)",
      "scopes": [],
      "parameters": {"params": {"type": "string", "required": false}}
    }
  ],
  "signals": [],
  "schedules": [],
  "required_scopes": [
    "store.read",
    "store.write",
    "http.request"
  ]
}
